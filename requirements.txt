fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0

# pip install -r requirements.txt fails with dependency resolution error. Project may not install cleanly.
# pydantic-settings v2 requires Pydantic v2. Code and dependencies must align.
# Code uses Pydantic v2 syntax (model_config, str | None, .model_dump()).

pydantic>=2.0.0
pydantic-settings>=2.0.0